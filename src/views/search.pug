extends base
include mixins/video

block content 
    form(methd="GET").search__form
        input(placeholder="Search by title",name="keyword", type="text")
        input(value="Search now", type="submit")
    
    div.video-grid
        each video in videos 
            +video(video)