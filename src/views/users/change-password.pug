extends ../base.pug

block content 
    h4=pageTitle
    if errorMessage
        span=errorMessage
    form(method="POST")
        input(placeholder="기존 비밀번호", type="password", name="oldPassword")
        input(placeholder="새로운 비밀번호", type="password", name="newPassword")
        input(placeholder="새로운 비밀번호 확인", type="password", name="newPassword2")
        input(value="비밀번호 변경",type="submit")
        hr
        